<UserControl x:Class="QuanLyNhaHang.Views.SoDoBanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:fa="clr-namespace:FontAwesome.Sharp;assembly=FontAwesome.Sharp"
             xmlns:controls="clr-namespace:QuanLyNhaHang.Controls"
             Background="{StaticResource BackgroundBrush}">
    <controls:PaginatedView Title="Danh sách Bàn chi tiết"
        PageSize="{Binding PageSize, Mode=TwoWay}"
        PageNumber="{Binding PageNumber, Mode=TwoWay}"
        TotalInfoText="{Binding TotalInfoText}"
        PrevPageCommand="{Binding PrevPageCommand}"
        NextPageCommand="{Binding NextPageCommand}"
        PageSizes="{Binding PageSizes}"
        PageNumbers="{Binding PageNumbers}">

        <controls:PaginatedView.ButtonContent>
            <StackPanel Orientation="Horizontal">
                <Button Style="{StaticResource ButtonPrimary}" Command="{Binding AddTableCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" Margin="0,0,10,0">
                <StackPanel Orientation="Horizontal">
                    <fa:IconBlock Icon="Plus" FontSize="14" Margin="0,0,8,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Thêm bàn" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
                <Button Content="Xóa" Style="{StaticResource ButtonDanger}" Margin="0,0,10,0"/>
                <Button Content="Sửa" Style="{StaticResource ButtonSecondary}" Margin="0,0,10,0"/>
                <Button Content="Tra cứu" Style="{StaticResource ButtonSecondary}" Margin="0,0,10,0"
                        Command="{Binding SearchTableCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
            </StackPanel>
        </controls:PaginatedView.ButtonContent>

        <controls:PaginatedView.MainContent>
            <DataGrid ItemsSource="{Binding Bans}" IsReadOnly="True" Style="{StaticResource GeneralDataGrid}"
                      ScrollViewer.CanContentScroll="True"
                      ScrollViewer.VerticalScrollBarVisibility="Visible"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="STT" Width="60" Binding="{Binding STT}"/>
                    <DataGridTextColumn Header="TÊN BÀN" Width="140" Binding="{Binding TenBan}"/>
                    <DataGridTextColumn Header="KHU VỰC" Width="*" Binding="{Binding KhuVuc}"/>
                    <DataGridTextColumn Header="SỐ CHỖ NGỒI" Width="120" Binding="{Binding SoChoNgoiText}"/>
                    <DataGridTextColumn Header="LOẠI BÀN" Width="120" Binding="{Binding TenLoaiBan}"/>
                    <DataGridTextColumn Header="PHỤ THU" Width="*" Binding="{Binding PhuThuText}"/>
                </DataGrid.Columns>
            </DataGrid>
        </controls:PaginatedView.MainContent>
    </controls:PaginatedView>
</UserControl>
