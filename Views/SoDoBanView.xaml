<UserControl x:Class="QuanLyNhaHang.Views.SoDoBanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fa="clr-namespace:FontAwesome.Sharp;assembly=FontAwesome.Sharp"
             xmlns:local="clr-namespace:QuanLyNhaHang.Views"
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="750"
             Background="{StaticResource BackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header and Action Buttons -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Title -->
            <TextBlock Grid.Column="0" Text="Danh sách Bàn chi tiết" FontSize="22" FontWeight="Bold" VerticalAlignment="Center"/>

            <!-- Actions -->
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button x:Name="BtnAdd" Content="+ Thêm bàn" Style="{StaticResource ButtonPrimary}" Command="{Binding AddTableCommand}" Margin="0,0,10,0"/>
                <Button x:Name="BtnDelete" Content="Xóa" Style="{StaticResource ButtonDanger}" Margin="0,0,10,0"/>
                <Button x:Name="BtnEdit" Content="Sửa" Style="{StaticResource ButtonSecondary}" Margin="0,0,10,0"/>
                <Button x:Name="BtnSearch" Content="Tra cứu" Style="{StaticResource ButtonSecondary}"/>
            </StackPanel>
        </Grid>

        <!-- Tables Grid -->
        <DataGrid Grid.Row="1" x:Name="dgTables" Style="{StaticResource GeneralDataGrid}"
                  ItemsSource="{Binding Bans}"
                  IsReadOnly="True"
                  ScrollViewer.CanContentScroll="True"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto">
            <DataGrid.Columns>
                <DataGridTextColumn Header="STT" Width="60" Binding="{Binding STT}"/>
                <DataGridTextColumn Header="TÊN BÀN" Width="*" Binding="{Binding TenBan}"/>
                <DataGridTextColumn Header="KHU VỰC" Width="150" Binding="{Binding KhuVuc}"/>
                <DataGridTextColumn Header="SỐ CHỖ NGỒI" Width="120" Binding="{Binding SoChoNgoiText}"/>
                <DataGridTextColumn Header="LOẠI BÀN" Width="120" Binding="{Binding TenLoaiBan}"/>
                <DataGridTextColumn Header="PHỤ THU" Width="150" Binding="{Binding PhuThuText}"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Pagination / Status Footer -->
        <Grid Grid.Row="2" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Page Size Dropdown -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Số dòng:" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,5,0"/>
                <ComboBox x:Name="CbPageSize" Width="80" Style="{StaticResource InputComboBox}"
                          ItemsSource="{Binding PageSizes}" SelectedItem="{Binding PageSize}"/>
            </StackPanel>

            <!-- Status Info -->
            <TextBlock Grid.Column="1" x:Name="txtTotalInfo" Text="{Binding TotalInfoText}" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="15,0,0,0"/>

            <!-- Pagination Buttons -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                <!-- Prev Button -->
                <Button x:Name="BtnPrevPage" Style="{StaticResource ButtonSecondary}" Padding="12,6" Margin="0,0,5,0"
                        Command="{Binding PrevPageCommand}">
                    <fa:IconBlock Icon="ChevronLeft" FontSize="12"/>
                </Button>

                <!-- Page Number Editable Dropdown -->
                <ComboBox x:Name="CbPageNumber" Width="70" Style="{StaticResource InputComboBox}" Margin="0,0,5,0"
                          ItemsSource="{Binding PageNumbers}" SelectedItem="{Binding PageNumber}"/>

                <!-- Next Button -->
                <Button x:Name="BtnNextPage" Style="{StaticResource ButtonSecondary}" Padding="12,6"
                        Command="{Binding NextPageCommand}">
                    <fa:IconBlock Icon="ChevronRight" FontSize="12"/>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
